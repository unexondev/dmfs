# Data Management — Practice Exam 5

20 questions · multiple choice (one correct answer) · based on Ch-8, Ch-9, Ch-10, Ch-14

---

**1. Secondary indices on a non-unique search key typically point to:**

- A) A bucket that contains pointers to the actual records
- B) The root of the B+-tree
- C) An overflow chain of leaves
- D) A single record only

**2. Big Data is commonly contrasted with traditional databases along three Vs:**

- A) Volume, Velocity, Variety
- B) Volume, Vertex, Value
- C) Vector, Velocity, Variety
- D) Volume, Value, Veracity

**3. In the term B+-tree, the B stands for:**

- A) Block
- B) Bucket
- C) Balanced
- D) Binary

**4. The two basic kinds of indices are:**

- A) Vector and raster indices
- B) Ordered indices and hash indices
- C) Primary indices and cookies
- D) Dense indices and bitmap indices

**5. A cookie is:**

- A) A bitmap
- B) A small piece of text containing identifying information, used to identify a session
- C) A compiled servlet class
- D) A disk block

**6. Key-value storage systems are often also called:**

- A) Buffer trees
- B) GIS databases
- C) OLAP cubes
- D) NoSQL systems

**7. In a distributed file system, each file block is typically replicated across:**

- A) All machines
- B) Ten machines
- C) Three machines
- D) One machine

**8. If the root of a B+-tree is not a leaf, it has at least:**

- A) 2 children
- B) 0 children
- C) n children
- D) n/2 children

**9. The type information of an object in RDF is provided by the:**

- A) DOM tree
- B) instance-of relationship
- C) primary key
- D) hash function

**10. Buffer trees are used in:**

- A) PostgreSQL Generalized Search Tree (GiST) indices
- B) Hadoop MapReduce
- C) Google File System
- D) Amazon DynamoDB

**11. In a two-layer web architecture:**

- A) There is no database
- B) Each request starts a new process via CGI
- C) The client connects directly to the database
- D) The web server and application server are combined into a single server

**12. The HTTP protocol is:**

- A) Connectionless
- B) Stateful by default
- C) A scripting language
- D) A storage engine

**13. Hadoop, the widely used open-source MapReduce implementation, is written in:**

- A) C++
- B) Go
- C) Python
- D) Java

**14. In a hash index vs a hash file-organization:**

- A) Both store only pointers
- B) A hash index stores pointers to records; a hash file-organization stores the records themselves
- C) Both store only records
- D) Neither uses a hash function

**15. HTTP is described as connectionless mainly to:**

- A) Enforce primary keys
- B) Improve image rendering
- C) Reduce load on the server, which has tight limits on open connections
- D) Compress JSON

**16. Which two data models are widely used for semi-structured data?**

- A) JSON and XML
- B) RDF and SQL
- C) CSV and YAML
- D) BSON and Avro

**17. XML marks up information using:**

- A) Tags enclosed in angle brackets
- B) Curly braces around key-value pairs
- C) Fixed-length binary fields
- D) Comma-separated values

**18. Using create unique index indirectly enforces that the search key is a:**

- A) Composite key
- B) Reduce key
- C) Foreign key
- D) Candidate key

**19. A line segment is represented in a database by:**

- A) A bitmap of pixels
- B) A hash of its length
- C) A single vertex
- D) The coordinates of its endpoints

**20. A hash function h maps:**

- A) Records to reduce keys
- B) Search-key values to bucket addresses
- C) Bucket addresses to disk blocks
- D) Tags to values

---

## Answer Key

1-A  2-A  3-C  4-B  5-B  6-D  7-C  8-A  9-B  10-A  11-D  12-A  13-D  14-B  15-C  16-A  17-A  18-D  19-D  20-B
